{% extends 'base.html.twig' %}

{% block title %}User Dashboard
{% endblock %}

{% block body %}
	<div class="container mx-auto text-center">
		<h1 class="text-3xl font-bold text-blue-600 mb-6">Welcome to Your Dashboard</h1>
		<p class="text-lg text-gray-700 mb-8">Manage your account and explore the features below:</p>

		<!-- Navigation Buttons -->
		<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 ">
			<a href="{{ path('app_reservation_index') }}" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
				View Reservations
			</a>
			<a href="{{ path('app_table_index') }}" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
				Manage Tables
			</a>
			<a href="{{ path('app_menu_index') }}" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
				Explore Menus
			</a>
			<a href="{{ path('app_restaurant_index') }}" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
				Restaurants
			</a>
		</div>
	</div>
{% endblock %}
